- Implemented walking skeleton (Issue #2).
- Created DinoSurvivors.Core class library with deterministic simulation, independent of MonoGame, and default SystemRng/NullPersistence/NullContentProvider seams.
- Created DinoSurvivors.Tests xUnit project with 4 passing tests covering player movement, diagonal normalization, and arena clamping.
- Integrated simulation core in Game1, mapping keyboard/mouse input to Control Actions, implementing follow camera, and rendering player and arena boundaries via programmatic white pixel texture.
- Configured roll-forward on all projects to run on .NET 10.
- Excluded Core/Tests compilation from root csproj to prevent duplicate attributes/types.
- Closed issue #2.
- Implemented Enemy spawning, Direct Pursuit, and Soft Collision for Compies (Issue #3).
- Added Enemy class with Position, Speed (120f), and Radius (12f) properties.
- Implemented deterministic spawning in Simulation: spawns Compies on a 1.5s interval outside of Player's MinSpawnRadius (450f) and clamps within ArenaSize (2000x2000).
- Implemented Direct Pursuit: enemies calculate direction to player and move straight towards them every step.
- Implemented Soft Collisions: resolves overlaps between enemies (50%/50% push) and between enemies and player (80% enemy push / 20% player push to avoid hard-blocking the player).
- Added 5 new headless xUnit tests verifying all spawning constraints, pursuit movement, enemy-enemy/enemy-player soft collisions, and boundary clamping.
- Updated Game1 to render Compies as small orange squares under the follow camera.
- Closed issue #3.
- Implemented Contact Damage + player HP + run-loss on death (Issue #4).
- Added PlayerMaxHp (100f), PlayerCurrentHp (100f), and IsRunLost properties to Simulation.
- Added Damage (10f), ContactCooldown (1.0f), and ContactCooldownTimer properties to Enemy.
- Updated Simulation.Step to decrement/clamp ContactCooldownTimer at the start of each step, apply contact damage on player overlap when the cooldown is 0, trigger the OnPlayerDamaged event, transition to IsRunLost = true when HP reaches 0, and early return (freeze normal play) if IsRunLost is true.
- Added 6 new headless xUnit tests covering initial HP, damage application, event dispatching, rate-limiting via contact cooldowns, multiple enemies dealing independent damage, HP 0 transitioning to lost run, and no play after run loss.
- Integrated visual feedback in Game1: player flashes red for 0.2s after taking damage, a screen-space HUD displays a glassmorphic health bar (turning red when below 30% HP), and a run-lost banner overlay covers the viewport when the run is lost.
- Closed issue #4.
- Implemented Tranq Pistol Auto-Fire + Mouse Aiming + Combat Feedback (Issue #5).
- Created Projectile class in DinoSurvivors.Core representing player weapon shots.
- Added properties Hp (default 20f), MaxHp (default 20f), and HitFlashTimer (default 0f) to Enemy class.
- Added Projectiles list, WeaponCooldown (0.8s), and events OnEnemyHit and OnEnemyKilled to Simulation.
- Updated Simulation.Step to:
  - Spawn Projectile at PlayerPosition towards actions.AimDirection on WeaponCooldown interval.
  - Move projectiles by speed (600px/s) and remove them if they go out of the 2000x2000 arena bounds.
  - Check collisions between projectiles and enemies (using Radius 6f for projectiles, 12f for enemies).
  - Apply 10 damage to enemies on hit, set HitFlashTimer to 0.15s, remove the projectile, and trigger OnEnemyHit.
  - Remove enemies when HP reaches 0 and trigger OnEnemyKilled.
- Added 5 new headless xUnit tests in SimulationTests.cs verifying weapon firing cadence, projectile movement direction and speed, projectile collision damage and removal, enemy death on 0 HP, and out-of-bounds projectile cleanup.
- Integrated visual and audio combat feedback in Game1:
  - Rendered projectiles as small yellow squares.
  - Rendered enemy hit flashes (Compy flashes white if HitFlashTimer > 0).
  - Programmed a custom 3x5 pixel font digits renderer to draw floating yellow damage numbers at hit positions (floating up and fading out).
  - Programmed a synthesized retro hit sound effect completely in code using DynamicSoundEffectInstance (no asset requirements).
  - Rendered a death pop particle effect at enemy death coordinates.
- Closed issue #5.
- Implemented XP Gems: drop, collect within Pickup Radius, accumulate (Issue #6).
- Created XpGem class representing experience gems dropped by defeated enemies.
- Added XpGems list, PlayerLevel (starts at 1), PlayerXp (starts at 0), XpToNextLevel (100 * level), PlayerPickupRadius (default 45f) properties and OnXpGemCollected event to Simulation.
- Updated Simulation.Step to drop an XP Gem at the enemy's coordinates on death, detect collection when player moves within PlayerPickupRadius, increment player levels on threshold crossings, and fire corresponding collection events.
- Added 5 new headless xUnit tests covering gem drop on death, collection only within Pickup Radius, player level up and XP roll-forward, and uncollected gem persistence.
- Integrated visual feedback in Game1: rendered XP gems as small cyan squares and drew a sleek cyan progress bar and level indicator in the screen-space HUD.
- Closed issue #6.
- Implemented Weapon roster (Flare Gun, Bug Zapper) + Weapon Levels + Weapon Slots (Issue #7).
- Created `WeaponDefinition`, `WeaponLevelData`, and `WeaponInstance` classes to represent weapon metadata, levels, and runtime instances.
- Refactored `IContentProvider` and `NullContentProvider`/`StubContentProvider` to supply data-driven weapon configurations, defining Tranq Pistol, Flare Gun, Bug Zapper, and a test Laser weapon under `DefaultContent`.
- Updated `Simulation` to initialize with Tranq Pistol (Lvl 1) in `EquippedWeapons`.
- Added `TryAddOrUpgradeWeapon` to `Simulation` to manage adding new weapons (slots capped at 3) and upgrading levels (capped at 5).
- Refactored `Projectile` to support custom speed, radius, damage, pierce count, and explosion radius, and added `HashSet<Enemy> HitEnemies` to prevent duplicate hits on the same enemy in a single frame.
- Updated `Simulation.Step` to update all equipped weapons: aimed weapons spawn projectiles in the aim direction, and autonomous weapons perform reverse-loop area checks to deal damage directly around the player.
- Added 4 new headless xUnit tests verifying slot/level limits, weapon-specific aimed/autonomous behaviors, Flare Gun piercing, and Bug Zapper range/damage.
- Integrated visual feedback in `Game1.cs`:
  - Rendered projectiles using their actual data-driven radius and color-coded them (Crimson for Flare Gun piercing shots, Yellow for Tranq Pistol).
  - Drawn equipped weapons and their levels in three distinct HUD slots using the custom pixel digit font.
  - Rendered a temporary translucent electric blue shockwave and outline around the player when the Bug Zapper discharges.
- Closed issue #7.
- Implemented Passive Items + Player Stat composition (Issue #8).
- Added PassiveLevelData, PassiveStat enum, PassiveDefinition, and PassiveInstance classes to DinoSurvivors.Core/Interfaces.cs (parallel to Weapon types).
- Extended IContentProvider with GetPassiveDefinition/GetAllPassives; updated NullContentProvider and StubContentProvider accordingly.
- Added five MVP passives to DefaultContent: FirstAidFannyPack (max HP ×1.15/1.30/1.50), FoamDinoClaw (damage ×1.15/1.30/1.50), RunningShoes (move speed ×1.15/1.30/1.50), EnergyDrink (cooldown ×0.85/0.70/0.55), SouvenirMagnet (pickup radius ×1.15/1.30/1.50).
- Added EquippedPassives list and TryAddOrUpgradePassive(string id) to Simulation (3-slot cap, 3-level cap).
- Added computed properties: PlayerEffectiveSpeed, PlayerEffectiveMaxHp, PlayerEffectivePickupRadius (base × passive multiplier via GetPassiveMultiplier helper).
- Updated Simulation.Step() to use PlayerEffectiveSpeed for movement, PlayerEffectivePickupRadius for gem pickup, and apply damage/cooldown passive multipliers at weapon fire time (baked into Projectile.Damage at spawn; applied to autonomous zapper damage; applied to CooldownTimer reset).
- Added 8 new headless xUnit tests covering: SouvenirMagnet increases pickup radius, passive slot cap of 3, passive level cap of 3, RunningShoes increases movement distance, FoamDinoClaw increases projectile damage, EnergyDrink fires weapon before base cooldown expires, FirstAidFannyPack increases effective max HP, and combined 3-passive stat composition.
- All 37 tests pass (29 pre-existing + 8 new).
- Closed issue #8.
- Implemented Level Up Choice: paused one-of-three Temporary Run Upgrades (Issue #9).
- Added UpgradeType enum (NewWeapon, WeaponUpgrade, NewPassive, PassiveUpgrade, CashFallback) and UpgradeOption class to Interfaces.cs.
- Added IsPausedForLevelUp, PendingLevelUpOptions, and UnbankedJurassicCash properties to Simulation.
- Added SelectLevelUpOption(int index): applies chosen option, increments PlayerLevel, rolls over XP, clears pause, and chains to next level if XP still exceeds new threshold.
- Added GenerateLevelUpOptions(): builds eligible pool (weapon/passive add/upgrade filtered by slot/level caps), draws 3 via seeded RNG with category balancing (avoids all-weapon or all-passive triple when alternative exists), fills remainder with CashFallback.
- Changed XP collection in Step() from auto-leveling loop to pause trigger: when PlayerXp >= XpToNextLevel, populates PendingLevelUpOptions and sets IsPausedForLevelUp; Step() returns early while paused.
- Updated existing PlayerAccumulatesXpAndLevelsUp test to match new paused behavior (player must call SelectLevelUpOption to complete the level-up).
- Added 9 new headless xUnit tests: threshold pauses sim with 3 options, Step() no-ops while paused, any option applies level-up and unpauses, NewWeapon option adds weapon, PassiveUpgrade option increments passive level, full weapon slots exclude NewWeapon, full passive slots exclude NewPassive, category balancing swaps a same-category pick, all-fallback when no eligible upgrades remain, CashFallback grants cash not healing, XP threshold grows each level.
- All 48 tests pass (38 pre-existing + 10 new/updated).
- Closed issue #9.
- Implemented Jurassic Cash collection (Unbanked) + HUD (Issue #11).
- Created JurassicCashDrop class in DinoSurvivors.Core (Position + CashValue, mirrors XpGem).
- Added JurassicCashDrops list, BankedJurassicCash property, and CashDropChance (20%) constant to Simulation.
- Added cash drop logic at all three enemy-kill paths in Step() (direct projectile hit, explosion radius, autonomous weapon): calls _rng.NextDouble() < 0.20 and appends a JurassicCashDrop at the enemy's position.
- Added pickup loop for JurassicCashDrops in Step() (mirrors XP gem loop): collects within PlayerEffectivePickupRadius and increments UnbankedJurassicCash.
- Set UnbankedJurassicCash = 0 when IsRunLost transitions to true (death clears unbanked cash).
- Added 5 new headless xUnit tests: low-RNG enemy kill drops cash, high-RNG kill does not, cash collected within pickup radius increases unbanked total, drop persists outside radius, dying clears UnbankedJurassicCash.
- Integrated visual feedback in Game1.cs: rendered JurassicCashDrops as small gold squares in world space; added Jurassic Cash HUD row after weapon slots showing Unbanked (gold) and Banked (yellow) amounts via pixel number renderer.
- All 53 tests pass (48 pre-existing + 5 new).
- Closed issue #11.
- Implemented Wave Schedule escalation + Live Enemy Cap + Pickup Merging (Issue #13).
- Added EnemyType enum (Compy, Raptor, Triceratops) to Interfaces.cs; refactored Enemy constructor to accept optional EnemyType and set speed/damage/HP/radius per type (Raptor: 160 spd, 15 dmg, 35 HP; Triceratops: 60 spd, 20 dmg, 120 HP; Compy unchanged).
- Added WavePhase class and DefaultContent.WaveSchedules dictionary: Stage 1 (0–8 min Compies only, 8+ min Compies 70%/Raptors 30% at 1.2s interval), Stage 2 (Raptors common; Triceratops from 4 min), Stage 3 (dense mixed from start at 0.9s interval).
- Added StageNumber (1–3, default 1), StageTimeElapsed, and LiveEnemyCap (80/100/120) properties to Simulation. GetCurrentPhase() and SelectEnemyType() wire wave schedule into SpawnEnemy(); SpawnEnemy now creates typed enemies.
- Added MergePickups() called at end of each Step(): clusters of 5+ XP gems or cash drops within 30f radius merge into a single higher-value pickup.
- Added 8 new headless xUnit tests: Raptor stats, Triceratops stats, Stage 1 early spawn → Compy, Stage 1 late spawn → Raptor (with high NextValue RNG), LiveEnemyCap blocks spawning, 5+ nearby gems merge, 5+ nearby drops merge, 4 nearby gems do not merge.
- All 61 tests pass (53 pre-existing + 8 new).
- Closed issue #13.
- Implemented Stage lifecycle: Stage Exit + Safehouse Break + multi-stage run (Issue #14).
- Added SafehouseRewardType enum (PartialHeal, BankedCashBonus, BonusXp) and SafehouseRewardOption class to Interfaces.cs.
- Added to Simulation: IsStageExitRevealed, SafehousePosition, ExitMarkerDirection (computed), IsPausedForSafehouseBreak, PendingSafehouseBreakOptions, IsRunComplete, and constants StageExitRevealTime (600f) / SafehouseZoneRadius (60f).
- RevealSafehouse() places a boundary-side Safehouse using _rng (edge + offset); called automatically when StageTimeElapsed >= 600s.
- EnterSafehouse() triggered at end of Step() when player is within SafehouseZoneRadius: banks Unbanked cash, clears enemies/XpGems/JurassicCashDrops/Projectiles, generates 3 reward options, sets IsPausedForSafehouseBreak.
- SelectSafehouseRewardOption(int index): applies chosen reward, advances StageNumber (1→2→3) with carry-forward (weapons, passives, HP, banked cash) and cleared stage state, or sets IsRunComplete = true after stage 3.
- Added IsPausedForSafehouseBreak to Step() early-return guard so simulation freezes during break.
- Added 14 new headless xUnit tests covering reveal timing, boundary placement, continued spawning post-reveal, ExitMarkerDirection, zone-entry banking, pause state, all three reward types, no-step-while-paused, each reward behavior, stage advance with carry-forward, abandoned pickups on exit, and IsRunComplete after stage 3.
- All 75 tests pass (61 pre-existing + 14 new).
- Closed issue #14.
- Implemented Heliport Stage + T-Rex Boss + Win Condition (Issue #16).
- Stage 3 safehouse no longer ends the run; instead it advances to Stage 4 (HeliportStageNumber=4) and spawns a T-Rex at BossArenaCenter (1000, 1000).
- Added TRex class to Interfaces.cs with phase-based attack state machine (3 phases by HP%: >66%=P1, >33%=P2, else P3), attack states (Idle/Charging/Roaring/TailSweeping/SummoningWaves), and phase-sequenced per-phase attack patterns.
- Added Boss Arena Lock-In: IsBossArenaLocked activates when player enters 600f radius; player is clamped inside until T-Rex is defeated.
- Win condition: after T-Rex defeated, a chopper zone appears at BossArenaCenter (80f radius); touching it sets IsRunComplete=true.
- T-Rex attacks: Charging (moves at 350f toward player), Roaring (15 DPS AoE within 200f), TailSweep (40 instant damage within 250f on enter), SummoningWaves (spawns 3 Raptors near T-Rex).
- Added 24 new headless xUnit tests covering all boss behaviors.
- All 99 tests pass (75 pre-existing + 24 new).
- Closed issue #16.
- Implemented Level Up Choice menu in view (Game1) (Issue #22).
- Added GetUpgradeOptionLabel helper in Simulation to generate capitalized upgrade labels (e.g. "FLARE GUN - LV 1->2" or "+25 JURASSIC CASH").
- Added 3 new unit tests in SimulationTests.cs to cover label generation for NewWeapon, WeaponUpgrade, and Passive/Fallback cases.
- Extended hand-rolled pixel font in Game1.cs to cover A-Z, space, +, -, and > symbols, replacing old digit-only arrays.
- Integrated dim modal overlay, title/subtitle, selection highlights, and mouse/keyboard selection input (1/2/3, W/S/Up/Down + Enter, and mouse clicks) inside Game1.cs.
- All 102 tests pass (99 pre-existing + 3 new).
- Closed issue #22.
- Implemented Save Data + Souvenir Shop + Permanent Upgrades (Issue #15) using /tdd.
- Defined SaveData and BestRunSummary classes and updated the IPersistence interface in Interfaces.cs.
- Implemented the JSON file-backed FilePersistence in DinoSurvivors.Core and memory-based serialization inside StubPersistence in TestDoubles.cs.
- Integrated IPersistence in Simulation constructor to load/initialize save data, and mapped BankedJurassicCash, IsAutoFireEnabled, and ShowFloatingDamageNumbers properties to delegating SaveData fields.
- Implemented shop methods: querying permanent upgrade ranks (GetPermanentUpgradeRank), price escalation calculation (GetPermanentUpgradeCost: 100 / 250 / 500 cash), and purchasing (BuyPermanentUpgrade).
- Applied permanent upgrade multipliers to player baseline stats (max HP, movement speed, pickup radius) and compounded them with active passive items.
- Integrated permanent upgrade multipliers to weapon firing step (projectile damage, weapon cooldowns, autonomous zapper damage).
- Added best run summary tracking (maximum stage, player level, survived time, and cash collected) and triggered automatic saves on run endings (win, loss, or quit).
- Connected the real FilePersistence("save.json") inside Game1.cs.
- Added 7 new headless xUnit tests covering round-trip serialization, save loading, shop purchase deduction, cost escalation, baseline stat composition, combat stat composition, and best run updating.
- All 109 tests pass (102 pre-existing + 7 new).
- Closed issue #15.
- Implemented Fire Mode Setting + Pause Menu + settings (Issue #10) using /tdd.
- Updated Simulation.Step to process actions.Pause, toggling the pause state and freezing the step update logic.
- Updated Game1.cs to support edge-triggered pause, confirm, and cancel inputs to avoid rapid toggling.
- Programmed a Pause Menu and a Settings sub-menu in Game1.cs update/draw loops:
  - The Pause Menu overlay presents "RESUME", "SETTINGS", and "QUIT RUN" options.
  - The Settings Menu overlay allows toggling "FIRE MODE" (between Auto and Manual fire) and "DAMAGE NUMBERS" (between ON and OFF).
  - Both screens support keyboard (1/2/3, W/S/Up/Down + Enter) and mouse click/hover selection.
  - Adjusting settings auto-saves changes to persistence via SaveSimulationData.
- Added 1 new headless test verifying pause/unpause state toggles and simulation freezing behavior.
- All 110 tests pass (109 pre-existing + 1 new).
- Closed issue #10.
- Implemented Field Pickups (timed temporary effects) (Issue #12) using /tdd.
- Defined FieldPickupType enum (DoubleDamage, SpeedBoost, Magnet), FieldPickup (ground-space item), and ActiveFieldEffect (active buff) classes.
- Added FieldPickups and ActiveFieldEffects lists to Simulation, along with the OnFieldPickupCollected event and SpawnFieldPickup injection method.
- Updated Simulation.Step to:
  - Check player-to-pickup collection within PlayerEffectivePickupRadius, firing OnFieldPickupCollected and adding the ActiveFieldEffect on collection.
  - Tick down and expire ground pickups (Lifetime) and active buffs (Duration) by deltaTime.
  - Periodically spawn random Field Pickups in the arena on a 45-second interval using _rng.
- Integrated temporary stat multipliers in effective stat properties (speed, pickup radius, max HP) and combat calculation steps (damage, weapon cooldowns).
- Updated EnterSafehouse to clear all ground pickups and active buffs at stage transitions.
- Added 6 new headless unit tests covering spawning, lifetime expiration, collection range/events, stat composition/multiplicative stacking, duration expiration, stage transitions, and periodic spawning.
- All 116 tests pass (110 pre-existing + 6 new).
- Closed issue #12.
- Updated PRD issue tracking and closed Issue #1 (PRD: DinoSurvivors PC MVP) as fully implemented.
- Implemented CI Slice 1: single-platform macOS release pipeline (Issue #18).
- Added `.github/workflows/release.yml`: triggers on push to main only, sets up .NET 9 SDK, runs `dotnet publish -r osx-arm64` self-contained single-file, marks the binary executable, archives the entire publish directory to DinoSurvivors-macOS.zip, and creates a GitHub Release tagged `v0.0.${{ github.run_number }}` with the short commit SHA in the title and macOS Gatekeeper workaround instructions in the release notes.
- Concurrency group cancels superseded in-progress runs. Workflow does NOT trigger on tag pushes to avoid loops.
- No new C# tests added (CI YAML only); all 116 existing tests still pass.
- Closed issue #18.
- Implemented CI Slice 2: Windows target + dedicated release job (Issue #19).
- Refactored `.github/workflows/release.yml` into three jobs: `build-macos` (macos-14, osx-arm64) and `build-windows` (windows-latest, win-x64) run in parallel — each publishes, archives, and uploads a zip artifact; a dedicated `release` job (ubuntu-latest, needs both builds) downloads both artifacts and creates ONE GitHub Release with both zips attached.
- Windows archive uses PowerShell `Compress-Archive`; release notes now cover both macOS Gatekeeper and Windows SmartScreen workarounds.
- Deleted obsolete `scripts/build-macos.sh` and `scripts/build-windows.sh`; the workflow is now the single source of truth for release builds.
- No new C# tests added (CI YAML only); all 116 existing tests still pass.
- Closed issue #19.
- Implemented CI Slice 3: test gate before builds (Issue #20).
- Added a `test` job to `.github/workflows/release.yml` that runs on `ubuntu-latest` and executes `dotnet test DinoSurvivors.Tests/`.
- Added `needs: test` to both `build-macos` and `build-windows` jobs so a failing test suite blocks artifact creation and the GitHub Release.
- No new C# tests added (CI YAML only); all 116 existing tests still pass.
- Closed issue #20.
